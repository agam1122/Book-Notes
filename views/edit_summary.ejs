<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Summary</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light text-dark">
  <div class="container py-5">
    <h2 class="mb-4">Edit Summary</h2>
    <form action="/edit/summary/<%= book.id %>" method="POST">
      <div class="mb-3">
        <label for="summary" class="form-label">Detailed Explanation</label>
        <textarea class="form-control" name="summary" id="summary" rows="10"><%= book.summary %></textarea>
      </div>
      <button type="submit" class="btn btn-primary">💾 Save Changes</button>
      <a href="/book/<%= book.id %>" class="btn btn-secondary ms-2">Cancel</a>
    </form>
  </div>
</body>
</html>
