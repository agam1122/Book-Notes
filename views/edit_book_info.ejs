<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Book Info</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light text-dark">
  <div class="container py-5">
    <h2 class="mb-4">Edit Book Info</h2>
    <form action="/edit/book/<%= book.id %>" method="POST">
      <div class="mb-3">
        <label for="title" class="form-label">Book Title</label>
        <input type="text" class="form-control" name="book_title" id="title" value="<%= book.book_title %>" required />
      </div>
      <div class="mb-3">
        <label for="author" class="form-label">Author</label>
        <input type="text" class="form-control" name="author" id="author" value="<%= book.author %>" required />
      </div>
      <div class="mb-3">
        <label for="date_read" class="form-label">Date Read</label>
        <input type="date" class="form-control" name="date_read" id="date_read" value="<%= book.date_read %>" required />
      </div>
      <div class="mb-3">
        <label for="rating" class="form-label">Rating (out of 5)</label>
        <input type="number" class="form-control" name="rating" id="rating" value="<%= book.rating %>" min="1" max="5" step="0.1" required />
      </div>
      <div class="mb-3">
        <label for="notes" class="form-label">Short Notes</label>
        <textarea class="form-control" name="notes" id="notes" rows="4"><%= book.notes %></textarea>
      </div>
      <button type="submit" class="btn btn-primary">💾 Save Changes</button>
      <a href="/book/<%= book.id %>" class="btn btn-secondary ms-2">Cancel</a>
    </form>
  </div>
</body>
</html>
