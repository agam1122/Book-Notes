<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= book.book_title %> - Details</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/styles/book_detail.css" />
  <style>
    .card {
      border-radius: 1rem;
      transition: box-shadow 0.3s ease;
    }
    .card:hover {
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    }
    .rating-badge {
      font-size: 1rem;
      padding: 0.5em 0.75em;
      border-radius: 0.5rem;
      background-color: #ffe58f;
      color: #8c6d1f;
    }
    .section-title {
      font-weight: 600;
      border-bottom: 2px solid #ddd;
      padding-bottom: 0.3em;
      margin-bottom: 1em;
    }
  </style>
</head>

<body class="bg-light text-dark">

  <div class="container py-5">
    
    <div class="d-flex justify-content-between align-items-center mb-4">
        <!-- Back Button -->
        <a href="/" class="btn btn-outline-secondary ">← Back to Home</a>

        <!-- 🗑️ Delete Book Button -->
        <form action="/delete/book/<%= book.id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this book?');" class="mb-0">
            <button type="submit" class="btn btn-danger">🗑️ Delete Book</button>
        </form>
    </div>

    <!-- Book Info Card -->
<div class="card mb-4 shadow-sm p-3">
  <div class="row g-4 align-items-center">
    <!-- Book Cover -->
    <div class="col-md-4 text-center">
      <img src="<%= book.cover_url %>" class="img-fluid rounded" alt="Book Cover" style="max-height: 400px; object-fit: contain;" />
    </div>

    <!-- Book Details -->
    <div class="col-md-8">
      <div class="card-body px-2 px-md-4">
        <h2 class="card-title mb-2"><%= book.book_title %></h2>
        <h5 class="card-subtitle mb-3 text-muted">by <%= book.author %></h5>
        <p class="mb-2"><strong>📅 Date Read:</strong> <%= book.date_read %></p>
        <p class="mb-3">
          <strong>⭐ Rating:</strong> 
          <span class="rating-badge"><%= book.rating %>/5</span>
        </p>
        <div>
          <h6 class="section-title">📝 Short Notes</h6>
          <p class="card-text" style="white-space: pre-wrap;"><%= book.notes %></p>
        </div>

        <!-- 🔽 Edit Button for Book Info -->
        <a href="/edit/book/<%= book.id %>" class="btn btn-outline-primary mt-3">✏️ Edit Info</a>
      </div>
    </div>
  </div>
</div>

<!-- Detailed Explanation Section -->
<div class="card shadow-sm p-4 bg-white">
  <h4 class="section-title">📝 Detailed Explanation</h4>
  <p class="card-text" style="white-space: pre-wrap;"><%= book.summary %></p>

  <!-- 🔽 Edit Button for Summary -->
  <a href="/edit/summary/<%= book.id %>" class="btn btn-outline-secondary mt-3">✏️ Edit Summary</a>
</div>


  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
